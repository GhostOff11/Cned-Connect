import {useState} from 'react';import {documents} from '../data/documents';
export default function Documents(){const [q,setQ]=useState('');const list=documents.filter(d=>(d.name+d.category).toLowerCase().includes(q.toLowerCase()));const download=d=>{const blob=new Blob([`CNED Connect\nDocument fictif : ${d.name}\nÉtudiant : Reyan Lassoued`],{type:'text/plain'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=d.name+'.txt';a.click();URL.revokeObjectURL(a.href)};return <><h1 className="page-title">Documents</h1><div className="field" style={{maxWidth:460}}><input placeholder="Rechercher un document…" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid grid-2">{list.map(d=><div className="card row space-between" key={d.id}><div><strong>{d.name}</strong><div className="muted">{d.category}</div></div><button className="btn secondary" onClick={()=>download(d)}>Télécharger</button></div>)}</div></>}
