import {exams} from '../data/exams';import {Badge} from '../components/UI';export default function Exams(){return <><h1 className="page-title">Calendrier des examens</h1><div className="grid grid-3" style={{marginTop:18}}>{exams.map(e=><div className="card" key={e.id}><div className="row space-between"><h3>{e.subject}</h3><Badge>{e.status}</Badge></div><p>{e.date} à {e.time}</p><p>{e.duration} • {e.type}</p><p className="muted">{e.location}</p></div>)}</div><div className="card" style={{marginTop:18}}><h3>Mes convocations</h3><button className="btn secondary" onClick={()=>alert('Téléchargement simulé : Convocation examens.pdf')}>Télécharger la convocation</button></div></>}
