import {useState} from 'react';import {load,save} from '../utils/storage';export default function Settings(){const [prefs,setPrefs]=useState(()=>load('bts-settings',{emailNotif:true,compact:false}));const toggle=k=>{const n={...prefs,[k]:!prefs[k]};setPrefs(n);save('bts-settings',n)};return <><h1 className="page-title">Paramètres</h1><div className="card" style={{maxWidth:720,marginTop:18}}><div className="list-item row space-between"><div><strong>Notifications par e-mail</strong><div className="muted">Préférence locale de démonstration</div></div><input type="checkbox" checked={prefs.emailNotif} onChange={()=>toggle('emailNotif')}/></div><div className="list-item row space-between"><div><strong>Affichage compact</strong><div className="muted">Réduit légèrement l'espacement</div></div><input type="checkbox" checked={prefs.compact} onChange={()=>toggle('compact')}/></div></div></>}
