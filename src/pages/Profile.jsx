import {useState} from 'react';import {DEMO_USER} from '../data/config';import {load,save} from '../utils/storage';
export default function Profile(){const [editing,setEditing]=useState(false);const [profile,setProfile]=useState(()=>load('bts-profile',DEMO_USER));const update=(k,v)=>setProfile({...profile,[k]:v});const persist=()=>{save('bts-profile',profile);setEditing(false)};return <><h1 className="page-title">Mon profil</h1><div className="card" style={{maxWidth:720,marginTop:18}}><div className="row" style={{marginBottom:20}}><div className="avatar" style={{width:64,height:64,fontSize:20}}>RL</div><div><h2 style={{margin:0}}>{profile.firstName} {profile.lastName}</h2><div className="muted">{profile.formation}</div></div></div>{editing?<><label className="field">Prénom<input value={profile.firstName} onChange={e=>update('firstName',e.target.value)}/></label><label className="field">Nom<input value={profile.lastName} onChange={e=>update('lastName',e.target.value)}/></label><label className="field">Email<input value={profile.email} onChange={e=>update('email',e.target.value)}/></label><button className="btn" onClick={persist}>Enregistrer</button></>:<><p><strong>Nom :</strong> {profile.firstName} {profile.lastName}</p><p><strong>Formation :</strong> {profile.formation}</p><p><strong>Statut :</strong> {profile.status}</p><p><strong>Formation à distance :</strong> Oui</p><p><strong>Année :</strong> {profile.year}</p><p><strong>Classe :</strong> {profile.className}</p><p><strong>Option :</strong> {profile.option}</p><p><strong>N° d'inscription :</strong> {profile.registrationNumber}</p><p><strong>Responsable pédagogique :</strong> {profile.pedagogicalLead}</p><p><strong>Email :</strong> {profile.email}</p><button className="btn" onClick={()=>setEditing(true)}>Modifier mon profil</button></>}</div></>}
