import {useState} from 'react';import {assignments} from '../data/assignments';import {Badge,Modal} from '../components/UI';
export default function Assignments(){const [filter,setFilter]=useState('Tous');const [sel,setSel]=useState(null);const list=filter==='Tous'?assignments:assignments.filter(a=>a.status===filter);return <><h1 className="page-title">Devoirs</h1><div className="filters">{['Tous','À faire','En cours','Rendus','Corrigé'].map(f=><button key={f} onClick={()=>setFilter(f)} className={`filter-btn ${filter===f?'active':''}`}>{f}</button>)}</div><div className="grid grid-2">{list.map(a=><div className="card" key={a.id}><div className="row space-between"><div><span className="muted">{a.subject}</span><h3>{a.title}</h3></div><Badge tone={a.status==='À faire'?'orange':a.status==='Corrigé'?'green':''}>{a.status}</Badge></div><p>Date limite : {a.due}</p>{a.grade&&<strong>Note : {a.grade}/20</strong>}<div><button className="btn" onClick={()=>setSel(a)}>Voir le devoir</button></div></div>)}</div>{sel&&<Modal title={sel.title} onClose={()=>setSel(null)}><p>{sel.description}</p><p><strong>Matière :</strong> {sel.subject}</p><p><strong>Date limite :</strong> {sel.due}</p></Modal>}</>}
