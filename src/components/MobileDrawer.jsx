import {Link} from 'react-router-dom';import {X} from 'lucide-react';
const links=[['/devoirs','Devoirs'],['/cahier','Cahier de texte'],['/notes','Notes'],['/examens','Examens'],['/vie-scolaire','Vie scolaire'],['/documents','Documents'],['/employabilite','Employabilité'],['/profil','Mon profil'],['/parametres','Paramètres']];
export default function MobileDrawer({onClose,logout}){return <div className="drawer" onClick={onClose}><div className="drawer-panel" onClick={e=>e.stopPropagation()}><div className="row space-between"><h2>Menu</h2><button className="icon-btn" onClick={onClose}><X/></button></div><div className="list">{links.map(([to,label])=><Link className="nav-item" style={{color:'#000000'}} key={to} to={to} onClick={onClose}>{label}</Link>)}</div><button className="btn danger" style={{width:'100%',marginTop:20}} onClick={logout}>Se déconnecter</button></div></div>}
